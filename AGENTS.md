
# Agents Guidelines

- You can't mix &optional with &key.
- Don't use t as a variable name.  Always use at least two character variable names.
- Don't make changes to the code unless I explicitly ask for them.  By default assume I typed "Don't change the code." in front of every query.


